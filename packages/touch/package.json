{
  "private": true,
  "type": "module",
  "name": "forma-touch",
  "scripts": {
    "build": "npm-run-all --parallel \"dist\" \"dist-*\"",
    "dist": "bun build --minify --outdir dist --target node source/index.ts",
    "test": "bun test --watch"
  }
}
